import 'package:flutter/material.dart';


class DescActivy extends StatelessWidget {
  const DescActivy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("J- world"),
      ),
    );
  }
}
